apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-api-config
data:
  # PostgreSQL outside the cluster (adjust if your host changes).
  PGHOST: "18.222.197.145"
  PGPORT: "5432"
  PGDATABASE: "appdb"
  PGUSER: "appuser"
