---
- name: Create test namespace
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Create namespace
      kubernetes.core.k8s:
        api_version: v1
        kind: Namespace
        name: aap-test
        state: present