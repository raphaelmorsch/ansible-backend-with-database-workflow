---
- name: Import key pair into AWS
  hosts: localhost
  gather_facts: false

  tasks:

    - name: Create key pair in AWS
      amazon.aws.ec2_key:
        name: "{{ key_name }}"
        key_material: "{{ ssh_public_key }}"
        state: present